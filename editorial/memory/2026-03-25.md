# 2026-03-25 Memory

## Session Summary

### Published
- **Ace Combat 7 VR** — Tier B, Framework-Based (UEVR injection)
  - Full campaign playable via community UEVR injection
  - Two-tier cockpit system (Tier 1 aircraft have full HUDs, Tier 2 have gaps)
  - Limited HOTAS support, high hardware demands
  - Added PSVR section covering 2019 official release (3 missions only)
- **Subnautica VR** — Tier B, Full VR Mod (SubmersedVR)
  - Dual-route: abandoned official VR + active SubmersedVR mod
  - Clear recommendation: install SubmersedVR or play flat-screen
  - Hero image generated correctly with gemini-image-simple

## Maya Hero Image Workflow — CORRECTED

**ROOT CAUSE IDENTIFIED:**
Maya's TOOLS.md referenced skill name `gemini-image-simple` but that skill is a **workspace skill**, not a global skill.

The skill exists at `/home/archangel/.openclaw/workspace/skills/gemini-image-simple/` and Maya needs to:
1. Read the SKILL.md to understand invocation
2. Call the Python script directly: `python3 /home/archangel/.openclaw/workspace/skills/gemini-image-simple/scripts/generate.py "prompt" output.png`

**Skill name is `gemini-image-simple` — it IS correct.**

Earlier confusion: The global skills folder has `nano-banana-pro` which also uses Google Imagen, but requires `uv` dependency. The workspace skill `gemini-image-simple` is preferred because it uses pure Python stdlib (no dependencies).

**LOCKED-IN WORKFLOW:**
```
Generate a hero image for the game [GAME NAME].

Use gemini-image-simple. Reference the CompoundVR brand guide at /design/compoundvr-brand-direction-v1.md for visual direction.

Save to: [full path to /site/public/images/games/[game-slug]-vr-hero.jpg]

Return the file path when done.
```

**Required Parameters:**
- Model: `ollama/kimi-k2.5:cloud` (specify explicitly)
- "Use gemini-image-simple" must be in the task
- "Reference the CompoundVR brand guide" must be in the task
- Save path must be explicit

**Verification After Generation:**
- Check file is valid JPEG (not corrupted/abstract)
- Check dimensions (1280x720 minimum)
- Convert PNG to JPG with PIL if needed
- Optimize to 100-300KB

**What NOT to do:**
- Do NOT use `nano-banana-pro` (requires uv dependency)
- Do NOT use `openai-image-gen` (no API key)
- Do NOT include narrative context about the game
- Do NOT include visual direction like "VR headset" or "faded silhouettes"
- Do NOT skip the `gemini-image-simple` instruction
- Do NOT accept Maya output that mentions OpenAI or local generation